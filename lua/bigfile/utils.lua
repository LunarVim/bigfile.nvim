-- local M = {}

-- function M.for_each_matching_rule(rules, filesize, callback)
--   for _, rule in ipairs(rules) do
--     if rule.size > filesize then
--       callback(rule)
--     else
--       return
--     end
--   end
-- end

-- return M
